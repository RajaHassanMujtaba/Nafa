// Supabase is not used in this project (using custom backend instead).
// This file is kept as a stub to avoid import errors.

export type UserProfile = {
  id: string
  full_name: string
  email: string
  phone: string
  country: string
  province: string
  city: string
  cnic: string
  trading_experience: string
  profession: string
  created_at: string
  avatar_url?: string
}

export const supabase = null
